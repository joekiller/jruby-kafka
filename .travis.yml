sudo: false
language: ruby
cache:
  - bundler
  - directories:
    - $HOME/.m2
rvm:
  - jruby-19mode
before_install:
  - bundle install
  - rake compile
  - test/script/setup.sh
after_script:
  - test/script/shutdown.sh
